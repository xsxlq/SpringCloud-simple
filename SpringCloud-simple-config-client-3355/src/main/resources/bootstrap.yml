#配置文件：系统级 》 用户级
spring:
  cloud:
    config:
      name: springcloud-config-client #需要从GitHub上读取的资源名称（没有后缀名）
      profile: dev #本次访问的配置项
      label: master
      uri: http://localhost:3344 #Config的服务端地址，通过3344服务端过去GitHub的服务地址